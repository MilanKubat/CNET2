<Window x:Class="OknovaAplikace.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OknovaAplikace"
        mc:Ignorable="d"
        Loaded="OknoNacteno"
        Title="MainWindow" Height="450" Width="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition/>
        </Grid.RowDefinitions>


        <ScrollViewer Grid.Row="1">
            <TextBox x:Name="TextoveOkno" Text="TextBox"  />
        </ScrollViewer>

        <ScrollViewer Grid.Row="1" Grid.Column="1">
            <TextBox x:Name="TextoveOknoDebug" Text="Diagnostické informace:" />
        </ScrollViewer>

        <Button x:Name="btnNacist" Content="Načíst" HorizontalAlignment="Left" Margin="50,0,0,0" VerticalAlignment="Center" Height="30" Width="85" Grid.Row="0" Click="btnNacist_Click"/>
    </Grid>
</Window>
